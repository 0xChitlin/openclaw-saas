# Server
PORT=4000

# Database
DB_PATH=./data/deskagents.db

# Auth
JWT_SECRET=change-me-to-a-random-secret
ADMIN_API_KEY=change-me-to-a-strong-api-key

# SMTP (for agent workers)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=noreply@example.com
SMTP_PASS=your-smtp-password

# IMAP (for email agent)
IMAP_HOST=imap.example.com
IMAP_PORT=993
IMAP_USER=inbox@example.com
IMAP_PASS=your-imap-password
